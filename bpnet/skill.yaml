schema_version: 1
id: bpnet
title: BPNet Workflows
path: bpnet
status: active
family: profile-prediction-and-attribution
tasks:
  - preprocessing
  - training
  - prediction
  - attribution
  - motif-analysis
triggers:
  - bpnet
  - bpnet-train
  - bpnet-shap
  - modisco
  - finemo
required_inputs:
  - input-data-schema
  - task-objective
optional_inputs:
  - bpnet-params
  - splits-json
  - chrom-sizes
constraints:
  - repository-pinned-runtime-is-python37-tf241
  - bias-source-and-smoothing-keys-required
  - use-modern-bpnet-cli-entrypoints
tools: []
priority_rules:
  - prefer-when-user-explicitly-mentions-bpnet-cli
  - prefer-when-task-needs-shap-or-motif-pipeline-integration
