interface:
  display_name: "s2f Genomics Agent"
  short_description: "Routes genomics tasks across specialized skills with grounded constraints."
  default_prompt: "Use the s2f genomics agent to route my request to the right skill and provide a runnable, constraint-aware workflow."
